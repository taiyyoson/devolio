## Implementation notes

TODO: the technical intricacies worth reading about — the decisions that were not
obvious, and why they went the way they did. This is the section that separates a
project list from a portfolio.

## Architecture

TODO: add a diagram. Open a fenced block tagged `mermaid` and it renders inline —
see `src/content/projects/nala.md` for a flowchart and
`src/content/projects/roblox-studio-mcp.md` for a sequence diagram.

## Data schema

TODO: table shapes, message formats, or state layout. Delete this section if the
project has no interesting data model.

## Results

TODO: what came out of it — measured, if possible.

## Process

TODO: build-process shots next to the finished product. Drop images in
`public/images/projects/` and reference them as
`![caption](/images/projects/name.png)`.
